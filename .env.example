# Email Configuration
# Admin email for system notifications
EMAIL_ADMIN=your-admin-email@example.com
EMAIL_PASS=your-admin-email-password

# Email Server Configuration
# IMAP settings for reading emails
EMAIL_HOST_IMAP=imap.your-email-provider.com
EMAIL_PORT_IMAP=993

# SMTP settings for sending emails
EMAIL_HOST_SMTP=smtp.your-email-provider.com
EMAIL_PORT_SMTP=465

# Email Authentication
# Email account for sending system emails
EMAIL_USER=your-system-email@example.com
EMAIL_PASSWORD=your-system-email-password

# Server Configuration
# Port on which the server will run
PORT=3000

# JWT Configuration
# Secret key for JWT token generation and verification
# Make sure to use a strong, unique secret in production
JWT_SECRET=your-jwt-secret-key

# MongoDB Configuration
# Connection string for MongoDB Atlas or local MongoDB instance
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=your-mongodb-connection-string

# Cloudinary Configuration
# Cloud storage settings for file uploads
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Note: 
# 1. Never commit the actual .env file to version control
# 2. Keep your production secrets secure and different from development values
# 3. Use strong passwords and secrets in production
# 4. Consider using a secrets management service in production 